# PeriodicTableDB
A PostgreSQL-based database modeling chemical elements of the periodic table. Includes a Bash script (element.sh) that retrieves element information by atomic number, symbol, or name.
